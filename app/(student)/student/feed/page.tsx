import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function StudentFeedPage() {
  return (
    <PagePlaceholder
      description="The shared question feed will land here in Phase 2, including tier badges, visibility badges, reset prioritization, and student-to-student participation later on."
      eyebrow="Shared Feed"
      primaryHref="/"
      primaryLabel="Go to home"
      secondaryHref="/student/profile"
      secondaryLabel="Open profile"
      title="Question feed scaffolded"
    />
  );
}
