import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function StudentAskPage() {
  return (
    <PagePlaceholder
      description="This route is now scaffolded for the Phase 2 question composer, including title, body, answer tier selection, and public or private answer visibility."
      eyebrow="Question Composer"
      primaryHref="/student/profile"
      primaryLabel="Open profile"
      secondaryHref="/"
      secondaryLabel="Back to home"
      title="Ask page coming next"
    />
  );
}
