## See parent CLAUDE.md

The authoritative project rules live in `../CLAUDE.md` at the project root.

Key reminders for this folder:
- The combined graph is at `../graphify-out/` (NOT inside `web/`).
- Read `../graphify-out/GRAPH_REPORT.md` first when answering codebase questions.
- A git post-commit hook auto-rebuilds the parent graph after every commit — do not rebuild manually.
